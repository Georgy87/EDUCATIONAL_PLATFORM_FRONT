import React from "react";
import { NavLink } from 'react-router-dom';

const Registration = () => {
    return (
        <div>
            <NavLink to="/login">
                Registration
            </NavLink>
        </div>
    );
};

export default Registration;
